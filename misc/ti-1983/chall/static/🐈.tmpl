<!-- this was autogenerated by ai im ngl, i got lazy and im not a web player so we autogenerate these, its pawbably garbage but ykwhat we dont talk about it -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Meow Page 🐈</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      background: linear-gradient(to right, #fdfcfb, #e2d1c3);
      font-family: 'Comic Sans MS', cursive, sans-serif;
      text-align: center;
      color: #444;
    }

    .cats {
      font-size: 3rem;
      animation: bounce 1.2s infinite alternate ease-in-out;
      margin-top: 2rem;
    }

    @keyframes bounce {
      from {
        transform: translateY(0);
      }
      to {
        transform: translateY(-20px);
      }
    }

    .meow {
      font-size: 4rem;
      color: #e67e22;
      text-shadow: 2px 2px #fff;
      margin: 2rem 0;
      animation: pulse 2s infinite;
    }

    @keyframes pulse {
      0%, 100% {
        transform: scale(1);
      }
      50% {
        transform: scale(1.1);
      }
    }

    .output {
      font-family: monospace;
      font-size: 1.5rem;
      color: #2c3e50;
      background: #f8f8f8;
      display: inline-block;
      padding: 1rem 2rem;
      border-radius: 12px;
      box-shadow: 2px 2px 10px rgba(0,0,0,0.1);
    }
  </style>
</head>
<body>
  <div class="cats">🐈🐈🐈🐈🐈</div>
  <div class="meow">meow</div>
  <div class="output">{{out}}</div>
</body>
</html>
